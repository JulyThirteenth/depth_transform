sensor_cfg:
  fov_deg: [90.0,90.0]
  dist_scale: 20.0

transform_cfg: # Poincloud以X右为正， Y上为正，Z前为正
  rotate_points: [] # ['z', -30]
  filter_points: [] # ['y', [-0.25, 0.25]]

projection_cfg:
  map_resolution: 0.2
  map_size: 100
